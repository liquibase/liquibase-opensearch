name: Build and Test

on:
  workflow_dispatch:
  pull_request:
    types:
      - opened
      - reopened
      - synchronize

permissions:
  contents: write
  pull-requests: write
  packages: read
  id-token: write
  
jobs:

  authorize:
    environment: ${{ github.event_name == 'pull_request_target' && github.event.pull_request.head.repo.full_name != github.repository && 'external' || 'internal' }}
    runs-on: ubuntu-latest
    steps:
      - run: "true"

  build-test:
    needs: authorize
    uses: liquibase/build-logic/.github/workflows/os-extension-test.yml@main
    secrets: inherit
    with:
      java: "[ 17, 21 ]"
      # The integration tests start OpenSearch via testcontainers, which needs a
      # Docker daemon. GitHub's windows-latest runners have none, so every IT
      # errors with "Could not find a valid Docker environment" there.
      os: '["ubuntu-latest"]'
      runIntegrationTests: true

  test-cli:
    needs: authorize
    runs-on: ubuntu-latest
    services:
      opensearch:
        image: opensearchproject/opensearch:3
        env:
          DISABLE_SECURITY_PLUGIN: true
          "discovery.type": single-node
        ports:
          - 9200:9200
          - 9600:9600
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up JDK
        uses: actions/setup-java@dd06d9cba3e5552c54d9f8ea23572deb30010f7c # v6.0.0
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Install Liquibase CLI
        run: |
          set -euo pipefail
          curl -LO https://github.com/liquibase/liquibase/releases/download/v4.33.0/liquibase-4.33.0.tar.gz
          mkdir liquibase
          tar -xzf liquibase-4.33.0.tar.gz -C liquibase

      - name: Build fat jar
        run: ./mvnw clean package

      - name: Install liquibase-opensearch in CLI
        run: cp target/liquibase-opensearch-*-fat.jar liquibase/lib/

      - name: Run Liquibase CLI
        shell: bash
        run: |
          set -euo pipefail
          liquibase/liquibase update --changelogFile src/test/resources/liquibase/ext/changelog.httprequest.yaml --url opensearch:http://localhost:9200 | tee cli-output.txt
          # make sure that our test executed the migration
          grep 'Run:.*1' cli-output.txt

  dependabot:
    needs: build-test
    uses: liquibase/build-logic/.github/workflows/dependabot-automerge.yml@main
    secrets: inherit
